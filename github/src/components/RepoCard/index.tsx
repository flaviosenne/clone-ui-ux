interface Props {
    username:string,
    reponame:string,
    description:string,
    language:string,
    starts:number,
    forks:number,
}

const RepoCard: React.FC<Props> = (props: Props) => {
    return (
        <></>
    )
}

export default RepoCard